---
description: Implement a bounded approved feature
argument-hint: "<feature-dir>"
---
Read `.pi/agents/implementer.md` and follow it with the project's authorities.
Feature directory: $1

If no feature directory was supplied, ask for it. Inspect approval and scope before
editing. This is one implementation assignment, not a request to spawn agents.
